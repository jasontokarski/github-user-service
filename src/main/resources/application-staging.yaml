logging:
  level:
    root: INFO
    com.branch.vcsuserservice: INFO

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,caches
  endpoint:
    health:
      show-details: when-authorized
